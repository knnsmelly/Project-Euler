def square(x):
  return x*x

n = 100

for i in range (n):  
  square_sum = square(sum(range(1, n+1)))
  sum_square = sum(int(square(x)) for x in range(1,n+1))
  
difference = square_sum - sum_square
print(difference)
